; Allow larger uploads for images/videos used by the CMS
upload_max_filesize = 128M
post_max_size = 128M
memory_limit = 256M
max_file_uploads = 50
max_input_time = 120
; Optional: keep temp files around long enough for move()
; sys_temp_dir can be customized if needed
